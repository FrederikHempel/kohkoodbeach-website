# Reference screenshots the design was built against — not part of the live site.
Visual inspiration/

# Every page loads the .webp twin; these originals are dead weight (see CLAUDE.md).
# Three files are the exception — the OG image and the two icons — kept below.
assets/**/*.jpg
assets/**/*.jpeg
assets/**/*.png
assets/**/*.mp4
assets/**/*.mov
!assets/og-image.jpg
!assets/logo/apple-touch-icon.png
# The booking page's hero loop is the one video the pages load.
!assets/book-hero.mp4

# OS/editor noise
.DS_Store

# Sandbox temp copies — two of these (3.5 MB of half-written book-hero.mp4)
# were committed and served publicly before anyone noticed.
*.sb-*
